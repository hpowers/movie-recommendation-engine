<center>

<br><br>

<%= render @movie %>

<br><br>

<%= link_to 'Next →', movie_path( @ranking + 1 ), :class => "btn btn-primary" %>

<br><br><br><br><br><br>

<%= form_for :theaters, url: movie_theaters_path(@id) do |f| %>

  <%= f.text_field :zip, :placeholder => "zip code" %> <br>
  <%= f.hidden_field :movie_id, :value => "id#{@movie.id}" %>
  <%= f.submit 'Get Showtimes', class: 'btn' %>

<% end %>

<br><br>
  
<%= debug(params) if Rails.env.development? %>


</center>